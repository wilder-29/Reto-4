 #Verificar si el código ASCII de la primera letra de una cadena (longitud 1) es par

def ascii_par(cadena):
    if len(cadena) == 1:
        return ord(cadena[0]) % 2 == 0
    else:
        return None 

caracter = input("Ingrese un carácter: ")
print("El código ASCII es par:", ascii_par(caracter))
